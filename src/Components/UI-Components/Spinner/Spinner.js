import React from "react";
import classes from "./Spinner.module.css";

const spinner = (props) => {
    return (
        <div attr="123" className={classes.loader}></div>
    );
};

export default spinner;
